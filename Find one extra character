#include <iostream>
using namespace std;
int main(void)
{
  string a,b;
  int y=0;
  cin>>a>>b;
  for(int i=0;i<b.size();i++)
  {
    y=a.find(b[i]);
    if(y!=string ::npos)
      continue;
    else 
      y=i;break;
    
  }
  cout << b[y] << endl;
  
  
}
