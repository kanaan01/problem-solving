#include <iostream>
using namespace std;
int main()
{
  double w,h;
  cin >> w >> h;
  double bmi=w/(h*h);
  if(bmi<18.5)
    cout << bmi << " Underweight";
  else if(bmi>=18.5 && bmi<25)
    cout << bmi << " Normal";
 else  if(bmi>=25 && bmi<30)
    cout << bmi << " Overweight";
  else 
    cout <<bmi << " Obese";

 return 0;
}
