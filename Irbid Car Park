#include <iostream>
#include <iomanip>
using namespace std;
int main(void)
{
  float a;
  float b;
  int c;
  cin >> a >> b >> c;
  cout << setprecision(2) << fixed << "Your total rate = " << a+b*(c-1) ;
  return 0;
}
