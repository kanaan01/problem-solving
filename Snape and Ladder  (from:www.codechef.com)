#include<iostream>
#include <cmath>
using namespace std;
int main() {
  int t,b,ls;
  float rsmin,rsmax;
  cin >>t;
  int i=1;
  while(i<=t)
 
  {
    cin >> b >> ls;
    rsmin =sqrt(ls*ls-b*b);
    rsmax =sqrt(ls*ls +b*b);
    cout << rsmin << " " << rsmax <<endl;
    i++;
  }


  return 0;
}
