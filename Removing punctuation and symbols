#include<iostream>
using namespace std;
string sig(string a){
  for(int i=0;i<a.size();i++)
  if(((a[i]>='A' &&a[i]<='Z')||(a[i]<='z'&&a[i]>='a')||a[i]==' '))cout<<a[i];
  return a;
}
int main()
{
  string a;
  getline(cin,a);
  a=sig(a);
}
